import React from "react";
import ReactDOM from "react-dom/client";
import GoldTokenPage from "./GoldToken";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GoldTokenPage />
  </React.StrictMode>,
);
